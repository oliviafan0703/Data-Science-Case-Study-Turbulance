---
title: "Case Study Report"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output: pdf_document
---

```{r setup, message=F, warning=F, echo=F}
require(rstan)
require(tidyverse)
require(rstanarm)
require(magrittr)
library(corrplot)
```

```{r load-data, message = FALSE}
data_train<-read_csv("data/data-train.csv")
data_test<-read_csv("data/data-test.csv")
```

## EDA

```{r}
plot(data_train$R_moment_1, data_train$R_moment_2)
plot(data_train$R_moment_1, data_train$R_moment_3)
plot(data_train$R_moment_1, data_train$R_moment_4)
plot(data_train$R_moment_2, data_train$R_moment_3)
plot(data_train$R_moment_2, data_train$R_moment_4)
plot(data_train$R_moment_3, data_train$R_moment_4)
```

```{r}
#plot(data_train[1], data_train[2])
?cor
```

```{r}
# St, Re, Fr
plot(data_train$St, data_train$Re)
plot(data_train$St, data_train$Fr)
plot(data_train$Re, data_train$Fr)
```


## Introduction


## Methodology

After loading the data, we performed exploratory data analysis on all three predictors and four moments:

We found that the moments are highly correlated:

```{r}
res<-cor(data_train)
res
corrplot(res, tl.col ="black")
```

We used an inverse-logit function to transform `Fr` due to the infinity values that exist in the data.

```{r}

lm.fit <- lm(R_moment_1 ~ St+ Re+St*Re+invlogit(Fr), data = data_train)
summary(lm.fit)
```

## Results

## Conclusion